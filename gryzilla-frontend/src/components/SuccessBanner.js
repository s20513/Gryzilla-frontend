export default function SuccessBaner({placeholder}) {

    return (
        <div className="d-flex justify-content-center" style={{color: "#50D975"}}>
            {placeholder}
        </div>
    );
}